const uiModule = (function(){
    const DOMElements = {
        timeLeft,
        wpm,
        cpm,
        accuracy,
        wpmChange,
        cpmChange,
        accuracyChange,
        textInput, nameInput,
        content, activeWord,
        modal
    };

    return {
        getDOMElements(){},
        updateTimeLeft: function(){},
    }
})();